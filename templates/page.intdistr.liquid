
<div class="bump">
<section id="blacktop">
  
  <div>
  
  <h1>{{ page.title }}</h1>
    

    
  </div>
  
  {% assign articles = blogs.international-distributors.articles | sort: 'title' %}
{% for article in articles %}
  <div class="container">
    <div id="{{ article.title | camelcase | downcase }}" class="scrollfix"></div>
      <div class="state-head">
        
        <h2>{{ article.title }}</h2>

{{ article.content }}
       
    </div>
  
  </div>
{% endfor %}

  
</section>
</div>

