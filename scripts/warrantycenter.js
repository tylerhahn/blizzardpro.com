import ReactDOM from "react-dom";
import React from "react";
import WarrantyCenter from "./components/WarrantyCenter";

const rootEl = document.querySelector("#react-embed");

rootEl && ReactDOM.render(<WarrantyCenter />, rootEl);
